import React, { useContext } from 'react'

const ProjectDetails = () => {
    const { projectContextData } = useContext(projectContextData);

  return (
    <div>ProjectDetails</div>
  )
}

export default ProjectDetails

